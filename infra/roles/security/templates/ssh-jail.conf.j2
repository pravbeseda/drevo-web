# Custom SSH jail configuration for fail2ban
# This file only contains custom settings that override the defaults

[sshd]
enabled = true
port = {{ ssh_port }}
filter = sshd
logpath = /var/log/auth.log
maxretry = 3
bantime = 86400  # ban for 1 day
findtime = 36000  # look back window (10 hours) 