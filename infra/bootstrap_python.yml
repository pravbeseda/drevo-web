- name: Install python3 on fresh hosts
  hosts: all
  gather_facts: false
  tasks:
    - name: Install python3 via raw shell
      ansible.builtin.raw: |
        apt update -y
        DEBIAN_FRONTEND=noninteractive apt install -y python3 python3-apt
      changed_when: true
